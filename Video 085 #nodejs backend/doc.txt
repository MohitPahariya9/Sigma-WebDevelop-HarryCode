-npm init -y    //no question asked direct install
-npm i --global nodemon  //for permanent setting of nodejs
-nvm use 16 // to work with version 16 nodejs
-node -v // to check version
-require() which is sync to chnge it package.json type = common to module shift chnge it for ecma script
-in common ==> const http =  require('node:http')
-in ecma ==> import http from "http" //similar to python
-we can use modules in browser..using...
     <script type="module" src="mymodule.js"></script> 
    <script type="module" src="main.js"></script>
